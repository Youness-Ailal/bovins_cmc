Build it using Next.js and Tailwind CSS. Infer layout, colors, typography, and spacing from the sketch and document every assumption you make. Create reusable components (Navbar, Footer, Button, etc.) in /components. When done, generate a MEMORY.md file at the project root that captures the design system (colors, fonts, spacing), every component created with its path and props, the folder structure, and a checklist for adding new pages.

Here is my pencil sketch for the first page: 
