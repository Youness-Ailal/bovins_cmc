# ── Server ──
PORT=3001
# Comma-separated when multiple frontends are allowed.
CLIENT_ORIGIN=http://localhost:3000

# ── Database ──
MONGO_URI=mongodb+srv://younessailal2:MRPqcAd0XAEVwXEv@cluster0.zqndisq.mongodb.net/?appName=Cluster0

# ── Auth (JWT) ──
# Generate a secret: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# ── BoviAI (Google Gemini) ──
# Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=